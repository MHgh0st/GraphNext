# راهنمای نگارش مستندات فنی — سامانه «فکر» (Fekr)

این سند، **قرارداد سبک نگارش** مستندات تحویل (Handover) سامانه «فکر» است. تمام مستندات `docs/` باید مطابق این راهنما نوشته شوند تا برای توسعه‌دهندگانی که بعداً به پروژه ملحق می‌شوند، یکجا، هم‌ریخت و قابل‌اتکا باشند.

---

## ۱. اصول کلی (Principles)

1. **زبان**: فارسی رسمی؛ اصطلاحات فنی استاندارد به انگلیسی (Endpoint، Store، LazyFrame و …) با ذکر معادل فارسی در اولین استفاده.
2. **دقت فنی**: مستندات باید با سورس‌کد **همگام** باشند؛ هر ادعا (مسیر فایل، نام تابع، پارامتر، اندپوینت) باید قابل ردیابی در کد باشد.
3. **تحویل‌محور**: خواننده باید بتواند بدون خواندن کد، رفتار، جریان داده، خطاها و وابستگی‌های هر ماژول را درک کند.
4. **عدم حذف محتوا**: در بازنویسی، مفاهیم فنی هرگز تغییر نمی‌کنند؛ فقط ساختار و بیان.
5. **مرجع نهایی**: هر مستند ماژول، خودکفا است؛ روابط بین ماژول‌ها با ارجاع (Link) به مستندات دیگر بیان می‌شود.

---

## ۲. ساختار سرفصل‌ها (Heading Hierarchy)

| سطح | استفاده |
|---|---|
| `#` | عنوان سند (یک‌بار در ابتدا) |
| `##` | بخش‌های اصلی شماره‌دار: `## ۱. هدف (Purpose)` تا بخش پایانی |
| `###` | زیربخش‌ها (مثلاً `### کامپوننت X`) |
| `####` | فقط در موارد ضروری (مثال JSON، جزئیات تابع) |

- بخش‌ها همیشه با اعداد فارسی و نام فارسی + نام انگلیسی شماره‌گذاری می‌شوند: `## ۳. داده ورودی (Input Data)`.
- زیربخش‌ها بدون شماره‌گذاری فارسی و با نام انگلیسی داخل backtick یا داخل پرانتز.

---

## ۳. هدر استاندارد (Document Header)

هر مستند ماژول با یک جدول متادیتا آغاز می‌شود (بعد از عنوان):

```
| مشخصه | مقدار |
|---|---|
| مسیر منبع | `BackEnd/app/services/ETL.py` |
| دسته | سرویس Backend |
| مستندات مرتبط | [۰۸-Extract-Transform-Load](backend/08-etl.md) |
```

- **مسیر منبع**: مسیر اصلی ماژول نسبت به ریشه پروژه (در صورت چندفایله بودن، مسیرهای اصلی با `،` جدا می‌شوند).
- **دسته**: سرویس Backend / روتر Backend / کامپوننت Frontend / صفحه Frontend / Store / ابزار کمکی / پیکربندی.
- **مستندات مرتبط**: ارجاع به مستندات مرتبط با عنوان و مسیر؛ در نبود مورد مرتبط، حذف می‌شود.

---

## ۴. قالب بخش‌ها (Section Templates)

### ۴.۱. قالب Backend (۱۱ بخش)

| # | بخش | محتوا |
|---|---|---|
| ۱ | هدف (Purpose) | نقش ماژول + جایگاه در زنجیره پردازش |
| ۲ | مسئولیت‌ها (Responsibilities) | جدول «مسئولیت / توضیح» |
| ۳ | فایل‌های مرتبط (Related Files) | جدول «نقش / مسیر» |
| ۴ | داده ورودی (Input Data) | جدول «آیتم / نوع / توضیح» + پارامترهای اندپوینت در صورت وجود |
| ۵ | منبع داده (Data Source) | دیتابیس/فایل/ماژول بالادستی |
| ۶ | مراحل پردازش (Processing Steps) | جدول «مرحله / شرح» شماره‌دار |
| ۷ | داده خروجی (Output Data) | جدول «فیلد / نوع / توضیح» + نمونه JSON در صورت لازم |
| ۸ | مصرف‌کنندگان خروجی (Consumers) | جدول «مصرف‌کننده / نحوه مصرف» |
| ۹ | وابستگی‌ها (Dependencies) | جدول «وابستگی / نوع» |
| ۱۰ | خطاهای احتمالی (Error Scenarios) | جدول «سناریو / رفتار فعلی» |
| ۱۱ | نمودار جریان داده (Data Flow Diagram) | Mermaid (sequenceDiagram یا flowchart) |
| — | خلاصه (Summary) | ۲ تا ۴ خط جمع‌بندی |

### ۴.۲. قالب Frontend (۱۰ بخش + خلاصه)

| # | بخش | محتوا |
|---|---|---|
| ۱ | هدف (Purpose) | نقش ماژول + جایگاه در رابط کاربری |
| ۲ | Props / ورودی (Input) | جدول «Prop / نوع / توضیح» |
| ۳ | منبع Props (Props Source) | جدول «منبع / Props یا State حاصل» |
| ۴ | استیت داخلی (Internal State) | جدول «State / پیش‌فرض / توضیح» + مشتق‌ها |
| ۵ | هوک‌های استفاده‌شده (Hooks Used) | جدول «هوک / کاربرد» |
| ۶ | فراخوانی‌های API (API Calls) | جدول «فراخوانی / اندپوینت / توضیح» |
| ۷ | تبدیل داده (Data Transformation) | جدول «تبدیل / شرح» |
| ۸ | خروجی رندر (Render Output) | ساختار درختی رندر + جدول وضعیت‌ها |
| ۹ | کامپوننت‌های استفاده‌کننده (Components Using It) | جدول «مصرف‌کننده / نحوه استفاده» |
| ۱۰ | نمودار جریان داده (Data Flow Diagram) | Mermaid (sequenceDiagram یا flowchart) |
| — | خلاصه (Summary) | ۲ تا ۴ خط جمع‌بندی |

> برای ماژول‌های غیرکامپوننت (مثل fetcher یا Store)، عنوان بخش‌ها با معادل مناسب نگهداری می‌شود (مثلاً «Props» برای توابع به «ورودی» تبدیل نمی‌شود؛ صرفاً محتوای همان بخش حفظ می‌شود).

---

## ۵. اصطلاحات استاندارد (Terminology Glossary)

متن مستندات باید از این نگاشت اصطلاحات پیروی کند؛ **معادل فارسی = مقدار داخل پرانتز**؛ بعد از اولین استفاده در هر سند، فقط از معادل انتخاب‌شده استفاده می‌شود:

| اصطلاح انگلیسی | معادل انتخابی در مستندات | یادداشت |
|---|---|---|
| Endpoint | اندپوینت | در اولین استفاده: «اندپوینت (Endpoint)» |
| Node | نود | «گره» استفاده نشود |
| Edge | یال | برای یال گراف؛ «لبه» استفاده نشود |
| Store | Store | همیشه انگلیسی |
| Router | روتر | |
| Service | سرویس | |
| Component | کامپوننت | |
| Hook | هوک | |
| Query Parameter | پارامتر Query | |
| LazyFrame / DataFrame | LazyFrame / DataFrame | همیشه انگلیسی |
| Variant | Variant | |
| Pareto coverage | پوشش Pareto | |
| Percentile | درصدک | |
| Histogram | هیستوگرام | |
| Dependencies | وابستگی‌ها | |
| Error Scenario | سناریوی خطا | |
| Handover | تحویل سامانه | |

---

## ۶. قواعد جدول‌ها (Table Conventions)

- هر لیست ساختاریافته با ۳ آیتم یا بیشتر **جدول** است (بندار‌بند یا بولت برای ۱–۲ آیتم).
- سرستون‌ها کوتاه و مشخص؛ ستون اول همیشه «نام» (فیلد/مرحله/سناریو/وابستگی).
- در جدول‌های فیلدها، نام فیلدها داخل backtick: `` `case_id` ``.
- نمونه JSON فقط وقتی که ساختار پیچیده است؛ با عنوان «نمونه پاسخ».

---

## ۷. قواعد نمودارها (Diagram Conventions)

- هر مستند ماژول **حداقل یک نمودار Mermaid** دارد (بخش ۱۱ بک‌اند / بخش ۱۰ فرانت‌اند).
- `sequenceDiagram` برای جریان‌های درخواست/پاسخ بین مؤلفه‌ها؛ `flowchart` برای پایداری‌ها و تصمیم‌ها.
- گره‌ها با نام فارسی + نام انگلیسی، و برچسب یال‌ها با اندپوینت یا نام تابع واقعی.
- نمودار نباید مرحله‌ای را نشان دهد که در متن نیست (و برعکس).

---

## ۸. ارجاع به کد (Code References)

- شناسه‌ها (نام تابع/فایل/متغیر/اندپوینت) همیشه داخل backtick.
- مسیر فایل‌ها به شکل `src/components/Graph.tsx` و `BackEnd/app/services/ETL.py` (نسبت به ریشه پروژه).
- در صورت لزوم، آدرس دقیق با شماره خط: `GraphData.py:170`.

---

## ۹. لحن و بیان (Tone)

- جملات کوتاه و خبری؛ از ضمایر «من/شما» پرهیز شود.
- از نقل‌قول مستقیم کد در متن خودداری شود (به‌جز شناسه‌ها).
- نکات هشدار با بلوک‌های `> ⚠️` و نکات تکمیلی با `> نکته:`.
- خلاصه پایان هر سند، «آنچه باید به خاطر بسپارید» را در ۲–۴ خط بگوید.

---

## ۱۰. نگارش خودکار (Checks)

قبل از تحویل هر سند:

1. `rg -n "Dern|اندرو|قسمت" <file>` → هیچ خروجی (تشخیص متن به‌هم‌ریخته)
2. هدر متادیتا بعد از عنوان وجود دارد.
3. بخش نمودار Mermaid وجود دارد و با متن هم‌خوان است.
4. اصطلاحات مطابق واژه‌نامه §۵ یکدست‌اند (مثلاً «گره» وجود نداشته باشد).
5. شماره بخش‌ها پیاپی و بدون پرش است.
